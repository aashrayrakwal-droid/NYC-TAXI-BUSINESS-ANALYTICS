The Power BI (.pbix) file is not included in this repository because it exceeds GitHub's file size limit.

The dashboard was built using the cleaned NYC Taxi dataset stored in PostgreSQL.

Dashboard screenshots are available in the `images/` folder.
